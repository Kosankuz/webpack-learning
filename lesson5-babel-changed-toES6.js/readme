5 Урок - добавляем babel.js


в webpack.config.js добавим следующее:

module: {
         loaders: [
             {
                 test: /\.js$/,
                 loader: 'babel-loader'  // babel makes code of the page readable for older browsers
             }
         ]
     }

Устанавливаем babel-loade:

npm i babel-loader
